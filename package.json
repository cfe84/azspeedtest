{
  "name": "azspeedtest",
  "version": "1.0.0",
  "description": "Tests the upload speed to Azure blob storage. This takes a URL to a blob container with a SAS in parameter, then tests speed for various scenarios",
  "main": "start.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "azspeedtest": "bin/azspeedtest"
  },
  "author": "cfe84",
  "license": "ISC",
  "dependencies": {
    "argv": "0.0.2",
    "async": "^2.6.0"
  }
}
